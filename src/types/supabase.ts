/**
 * GENERATED FILE — placeholder, do not edit by hand.
 *
 * Regenerate once a schema exists (docs/SUPABASE.md §7.4):
 *   supabase gen types typescript --project-id "$SUPABASE_PROJECT_REF" --schema public > src/types/supabase.ts
 *
 * `src/types/supabase.ts` is the single source of DB truth, gated in CI so a
 * drift fails the build. This placeholder keeps the three Supabase clients
 * (`src/lib/supabase/*`) type-checked until the schema is migrated.
 */
export type Database = {
  public: {
    Tables: Record<string, never>;
    Views: Record<string, never>;
    Functions: Record<string, never>;
    Enums: Record<string, never>;
    CompositeTypes: Record<string, never>;
  };
};
